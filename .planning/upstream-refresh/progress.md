# Progress

Read global/repository/desktop/CLI instructions. Retrieved Hermes context as historical data.
Official HTTPS fetch failed with rate limit. Trying normal authenticated SSH transport.
Official main c8aa5608c2 verified via GitHub commits API; local object matches exactly.
HTTPS fetch was rate limited; SSH fetch stalled and only owned fetch processes were stopped.
Merge 044acbdb00 retains local features and all current upstream ancestry. Two textual conflicts resolved in queue hook/tests.
Combined shared retry budget/manual recovery with upstream bounded retry scheduling and cancellation.
Adjusted upstream status-stack test to existing per-session scroll API. Broad regression run exposed older tests assuming no live history disclosure and hidden todos; updated behavioral expectations to the delivered UI. Brief fixture now supplies the actual actions-list response.
Desktop 88 of 92 files passed initially; all four failures now pass in focused reruns. Typecheck passed.
TUI 149 tests passed after building local Ink and replacing a stale copied node_modules package with the correct workspace symlink; old copy backed up in task work.
Python 1297 tests passed so far; custom-provider file remains running. Production desktop build underway.

Final build 044acbdb00 and official package succeeded. Python completed: 1367 passed, 1 Windows-only skip, 43 files, 261.3 seconds. TUI bundle built and 149 tests passed. Staged native folder/task flow passed and screenshot inspected. Full installed package swapped reversibly; receipt saved.
Four profile quick snapshots completed. Official gateway restart drained PID2487, now systemd PID3659206 is active on code_sha044acbdb00, matching desktop. No model settings changed.
Installed chat reload QA is being finalized. Two harness failures were an invisible duplicate title after reload and then an overbroad scratch replacement that removed the initial selection. Corrected the scratch script, not product code.

Installed native verification passed: saved chat after reload, folder flag after reopen and correct task link/card. Zero page errors. All owned native QA runs exited. Final report outputs/hermes-upstream-update.md. No further code changes after clean packaged merge.
